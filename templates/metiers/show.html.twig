{% extends 'base.html.twig' %}

{% block title %}Metiers{% endblock %}

{% block body %}
    <h1>Metiers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metier.id }}</td>
            </tr>
            <tr>
                <th>Competences</th>
                <td>{{ metier.competences ? metier.competences|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>Activites</th>
                <td>{{ metier.activites ? metier.activites|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_metiers_index') }}">back to list</a>

    <a href="{{ path('app_metiers_edit', {'id': metier.id}) }}">edit</a>

    {{ include('metiers/_delete_form.html.twig') }}
{% endblock %}
